[project]
name = "mqtt-docker-service"
version = "0.1.0"
description = "Dockerized MQTT telemetry publisher"
requires-python = ">=3.11"
dependencies = [
    "paho-mqtt>=2.1.0",
    "python-dotenv>=1.2.3",
]
